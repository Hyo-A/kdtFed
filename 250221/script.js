// 반복문 & 배열
// 문자열 => 정규표현식 => 배열
// 문자열 => 유사배열
// length & index

const str = "hello, everyone";
// console.log(str.split(""));
// split(" ")과 split("")은 다르다, 전자는 [ 'hello,', 'everyone' ]를 반환

//console.log(str.split(""));
// 1. 문자열을 배열로 변환하는 방법

// console.log([...str]);
// 2. 문자열을 배열로 변환하는 방법
//split("")과 같은 값을 반환

// console.log(Array.from(str));
// 3. 문자열을 배열로 변환하는 방법
//split("")과 같은 값을 반환
