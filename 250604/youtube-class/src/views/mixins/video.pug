mixin video(video)
  div
    h4
      a(href=`/videos/${video.id}`)=video.title
    ul 
      li ⭐#{video.rating}/5.
      li Comment : #{video.comment}
      li Posted : #{video.createdAt}
      li Views : #{video.views}